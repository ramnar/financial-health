# Dashboard
streamlit>=1.32.0
plotly>=5.20.0
pandas>=2.2.0

# PDF parsing
pdfplumber>=0.11.0
pikepdf>=8.0.0

# Config
python-dotenv>=1.0.0

# Gmail via IMAP (imaplib + email are Python stdlib — no extra packages needed)
